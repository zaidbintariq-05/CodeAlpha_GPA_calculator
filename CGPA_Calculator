#include <iostream>
#include <fstream>
#include <string>
using namespace std;

class Course {
public:
    string courseName;
    float gradePoint;
    int creditHours;
};

int main() 
{
    Course courses[20];
    int numberOfCourses;
    float totalGradePoints = 0;
    int totalCredits = 0;

    cout << "Enter number of courses: ";
    cin >> numberOfCourses;

    for (int i = 0; i < numberOfCourses; i++) {
        cin.ignore();

        cout << "\nEnter course name: ";
        getline(cin, courses[i].courseName);

        cout << "Enter grade point: ";
        cin >> courses[i].gradePoint;

        cout << "Enter credit hours: ";
        cin >> courses[i].creditHours;

        totalGradePoints += courses[i].gradePoint * courses[i].creditHours;
        totalCredits += courses[i].creditHours;
    }

    float cgpa = totalGradePoints / totalCredits;

    ofstream file("cgpa_result.txt");

    cout << "CGPA Result";
    file << " CGPA Result ";

    for (int i = 0; i < numberOfCourses; i++) {
        cout << courses[i].courseName
             << " Grade Point: " << courses[i].gradePoint
             << " Credit Hours: " << courses[i].creditHours << endl;

        file << courses[i].courseName
             << " Grade Point: " << courses[i].gradePoint
             << " Credit Hours: " << courses[i].creditHours << endl;
    }

    cout << "\nTotal Credits: " << totalCredits << endl;
    cout << "Final CGPA: " << cgpa << endl;

    file << "\nTotal Credits: " << totalCredits << endl;
    file << "Final CGPA: " << cgpa << endl;

    file.close();

    cout << "\nResult saved in cgpa_result.txt\n";

    return 0;
}
